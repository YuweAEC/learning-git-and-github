# TO add -

```bash
git add [ <files> or < . > (to add all files)]  # adds files to the staging area
```

```bash
git commit -m "message"  # Used to commit files (locally or on remote) on the repositorys
```

